{% extends 'back.html.twig' %}

{% block title %}LigneCommande{% endblock %}

{% block body %}
    <h1>LigneCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_commande.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_commande.quantite }}</td>
            </tr>
            <tr>
                <th>Nom Produit</th>
                <td>{{ligne_commande.produit.NomProduit}}</td>
            </tr>
            <tr>
                <th>Prix Produit</th>
                <td>{{ligne_commande.produit.PrixProduit}}</td>
            </tr>
        </tbody>
    </table>
    <div class="container my-3 bg-light">
    <div class="col-sm-4 col-md-2 text-center">
        <a href="{{ path('ligne_commande_index') }}"><button type="button" class="btn btn-block btn-primary"></i>Back To List</button></a>
    </div>
    </div>
    <div class="container my-3 bg-light">
    <div class="col-sm-4 col-md-2">
        <a href="{{ path('ligne_commande_edit', {'id': ligne_commande.id}) }}"><button type="button" class="btn btn-block btn-primary"><i class="fa fa-edit"></i>Edit</button></a>
    </div>
    </div>
    {{ include('ligne_commande/_delete_form.html.twig') }}
{% endblock %}
