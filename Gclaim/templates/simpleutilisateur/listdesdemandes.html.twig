{% extends 'back.html.twig' %}
{% block body %}
    <!-- /.row -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">LES DEMMANDES POUR DEVENIR COACH</h3>
                </div>
                <!-- ./card-header -->
                <div class="card-body">
                    <table class="table table-bordered table-hover">
                        <thead>
                        <tr>
                            <td>Id</td>
                            <td>USER_NAME</td>
                            <td>PASSWORD</td>
                            <td>EMAIL</td>
                            <td>Full_NAME</td>
                            <td>SUPPRIMER</td>
                            <td>ACCEPTER </td>
                            <td>REFUSER </td>

                        </tr>
                        </thead>
                        <tbody>
                        {% for s in l %}
                            <tr data-widget="expandable-table" aria-expanded="false">
                                <td>{{ s.id }}</td>
                                <td>{{ s.username }}</td>
                                <td>{{ s.password }}</td>
                                <td>{{ s.email }}</td>
                                <td>{{ s.fullname }}</td>
                                <td> <a href="{{ path('deletesimpleutilisateur1',{'id':s.id}) }}"class="btn btn-block btn-warning btn-sm">Delete</a></td>

                                <td> <a href="{{ path('devenircoach',{'id':s.id}) }}"class="btn btn-block btn-warning btn-sm">accepter</a></td>
                                <td> <a href="{{ path('annulerlademande',{'id':s.id}) }}"class="btn btn-block btn-warning btn-sm">refuser</a></td>

                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->
{% endblock %}