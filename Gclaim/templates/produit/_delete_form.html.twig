<form method="post" action="{{ path('produit_delete', {'id_produit': produit.Id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.Id) }}">
    <button class="btn">Delete</button>
</form>
