{% extends 'back.html.twig' %}
{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">LES COACHS</h3>
                </div>
                <!-- ./card-header -->
                <div class="card-body">
                    <table class="table table-bordered table-hover">
                        <thead>
                        <tr>
                            <td>Id</td>
                            <td>DATE</td>
                            <td>USERNAME</td>
                            <td>SIMPLE UTILISATEUR</td>

                            <td>SUPPRIMER</td>
                            <td>MODIFIER</td>

                        </tr>
                        </thead>
                        <tbody>
                        {% for s in rdv %}
                            <tr data-widget="expandable-table" aria-expanded="false">
                                <td>{{ s.id }}</td>
                                <td>{{ s.date|date }}</td>
                                <td>{{ s.coach.username }}</td>
                                {# <td>{{ s.user.id }}</td> #}
                                <td></td>
                                 <td> <a href="{{ path('rdv_delete',{'id':s.id}) }}">Delete</a></td>
                                <td>  <a href="{{ path('rdv_edit',{'id':s.id}) }}">Update</a></td>



                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->
{% endblock %}